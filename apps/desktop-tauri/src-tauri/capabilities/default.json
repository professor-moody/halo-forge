{
  "identifier": "default",
  "description": "Allow the desktop shell to run only the bundled Halo Forge sidecar.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
