[package]
name = "halo-forge-desktop"
version = "0.1.0"
description = "Halo Forge desktop workstation app"
authors = ["Halo Forge"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
