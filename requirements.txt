# halo-forge requirements (convenience mirror)
#
# Authoritative dependency metadata lives in `pyproject.toml`.
# Preferred install paths:
#   - Core runtime: `pip install .`
#   - Modality extras: `pip install .[audio]`, `.[vlm]`, `.[reasoning]`, etc.
# This file mirrors core + commonly used extras for environments that still use
# `pip install -r requirements.txt`.

# Core runtime (`project.dependencies`)
numpy>=1.24.0,<3.0.0
torch>=2.1.0,<3.0.0
transformers>=4.49.0,<6.0.0
peft>=0.13.0,<1.0.0
datasets>=2.14.0,<4.0.0
trl>=0.12.0,<1.0.0
accelerate>=0.26.0,<2.0.0
pyyaml>=6.0
requests>=2.31.0
tqdm
jsonlines
tensorboard
pandas
rich
fastapi>=0.115.0,<1.0.0
uvicorn>=0.34.0,<1.0.0
keyring>=25.0.0,<26.0.0

# Inference extras (`.[inference]`)
bitsandbytes
scipy
sentencepiece
protobuf
einops

# Benchmark extras (`.[benchmark]`)
# VLMEvalKit is GitHub-only; install with:
# pip install git+https://github.com/open-compass/VLMEvalKit.git

# VLM extras (`.[vlm]`)
qwen-vl-utils
ultralytics
easyocr

# Audio extras (`.[audio]`)
openai-whisper
torchaudio>=2.0.0
librosa>=0.10.0
jiwer>=3.0.0
soundfile

# Reasoning extras (`.[reasoning]`)
sympy>=1.12

# Plotting extras (`.[plot]`) — used by scripts/plot_*.py and `halo-forge plot`.
# Not a core dependency: nothing under halo_forge/ or ui/ imports it.
matplotlib

# Corpus extraction extras (`.[corpus]`)
beautifulsoup4>=4.12.0
pypdf>=5.0.0
python-docx>=1.1.0

# Dev extras (`.[dev]`)
pytest
httpx
psutil
Pillow>=10.0.0
jsonschema
pyinstaller>=6.0.0,<7.0.0
black
isort
ruff

# Optional LLM backend extras (`.[llm-backends]`)
# anthropic>=0.18.0
# openai>=1.0.0

# Note: legacy TUI is deprecated and not part of the supported runtime surface.
