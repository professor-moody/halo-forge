# halo-forge requirements
# =============================================================================
# Note: torch is provided by the custom toolbox with ROCm support
# If running outside toolbox, install torch separately with ROCm
# =============================================================================

# --- Core ML Stack ---
transformers>=4.46.0
peft>=0.13.0
datasets>=2.14.0
trl>=0.12.0
accelerate
safetensors
sentencepiece
huggingface-hub
einops
scipy
protobuf

# --- Quantization (built from source in toolbox) ---
# bitsandbytes  # Built with gfx1151 support in Dockerfile

# --- Data Processing ---
requests>=2.31.0
pyyaml>=6.0
tqdm
jsonlines
pandas

# --- Monitoring & Logging ---
tensorboard
wandb

# --- Visualization ---
matplotlib
seaborn

# --- Remote Verification ---
paramiko

# --- CLI & TUI ---
rich
textual>=0.50.0

# --- Testing ---
pytest

# --- Optional: LLM backends for data generation ---
# anthropic>=0.18.0
# openai>=1.0.0
