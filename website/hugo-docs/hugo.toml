baseURL = "https://halo-forge.io/"
languageCode = "en-us"
title = "halo forge"
enableRobotsTXT = true

[params]
  description = "Cross-vendor local finetuning workstation — SFT, DPO, GRPO, RAFT, RM with verifier-grounded rewards on ROCm, CUDA, Apple MLX, Apple MPS."
  github = "https://github.com/professor-moody/halo-forge"
  version = "1.4.0"

[markup]
  [markup.highlight]
    style = "monokai"
    lineNos = false
    codeFences = true
  [markup.goldmark.renderer]
    unsafe = true
