All-module fixture packs for coding + non-coding readiness checks.

`v1` is deterministic and contract-focused. It contains minimal artifacts required for
strict readiness validation in `scripts/run_all_module_matrix.py`.
